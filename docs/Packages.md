# Packages

We have the platform and the features. 

So the dependencies must be:
- platform -> external dependencies
- features -> platform or external dependencies

- **FORBIDDEN**: dependency platform -> features