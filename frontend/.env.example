# API Configuration
VITE_API_BASE_URL=http://localhost:8000/api

# App Configuration
VITE_APP_NAME=Modern tribes app
VITE_APP_VERSION=1.0.0

# i18n Configuration (supported: en, fr)
VITE_DEFAULT_LANGUAGE=en

# Guitar Notes — base URL where MP3 samples are served (no trailing slash)
# Leave empty to use the built-in Karplus-Strong synthesis fallback
VITE_GUITAR_SAMPLES_URL=