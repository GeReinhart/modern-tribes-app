// Re-exported from the platform level, which owns this component so other features (e.g. recipes)
// can show a read-only level badge without depending on the guitar feature.
export { LevelBadge } from '@/app/platform/core/layout/themes/components/LevelBadge.tsx';
