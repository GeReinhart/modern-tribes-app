// Re-exported from the platform level, which owns this gradient so other features (e.g. recipes)
// can reuse the same 0-5 difficulty color scale without depending on the guitar feature.
export { DIFFICULTY_LEVEL_STYLES } from '@/app/platform/core/layout/themes/components/difficultyLevelStyles.ts';
